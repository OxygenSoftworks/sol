services:
  - type: web
    name: sol-proxy
    env: go
    region: oregon
    plan: free
    buildCommand: cd sol/relay-backend && go build -o sol-relay
    startCommand: cd sol/relay-backend && ./sol-relay
    envVars:
      - key: PORT
        value: 8080
